# Integration test boundary

Unit 1 has no cloud integration tests. Later tests placed here must be explicitly isolated,
must never rely on production credentials, and must document their external prerequisites.

